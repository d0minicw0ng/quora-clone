<h3>Unanswered Questions</h3>

<% if !@questions.empty? %>
	<ul>
		<% @questions.each do |question| %>
			<li>
				<%= link_to question.title, question_url(question) %>
			</li>
			<br />
		<% end %>
	</ul>
<% else %>
	<strong>There are no unanswered questions at the moment.</strong>
<% end %>