<ul class="nav nav-pills">
  <li class="active"><%= link_to "Top Stories", new_question_url %></li>
  <li><%= link_to "Most Recent", new_question_url %></li>
</ul>

<div id="top_stories">
	<ul>
		<% @questions.each do |question| %>
		 	<li data-id="<%= question.id %>">
			  <%= link_to question.title, question_url(question) %>
			</li>
			<hr width="80%">
	  <% end %>
	</ul>
</div>