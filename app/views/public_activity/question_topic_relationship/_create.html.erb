was added to the topic

<% if activity.trackable %>
  <%= link_to activity.trackable.topic.name,
		topic_url(activity.trackable.topic) %>
	<%= time_ago_in_words(activity.trackable.created_at) %> ago
<% else %>
	which the relationship has since been removed
<% end %>

